pipeline{
    agent any
    stages{
        stage("git Checkout")
            {
                 git url: 'https://github.com/vignesh810/microservice.git', branch: 'main'
                 sh 'ls -l'
            }
        
    }    
    
        }
